function oddOrEven(array) {
    //enter code here
   if (array.length === 0) {
    return 'even'
   } else if (array.length === 1 && array[0] === 0) {
    return 'even'
   } else {
    let num = array.reduce((acc, el) => acc + el, 0)
    return num % 2 === 0 ? 'even' : 'odd'
   }
}
